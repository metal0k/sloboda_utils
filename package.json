{
  "name": "sloboda-map",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Interactive map of Andreyevskaya Sloboda",
  "scripts": {
    "generate-houses": "tsc scripts/generate-houses.ts --target ES2022 --module ESNext --moduleResolution Bundler --strict --esModuleInterop --skipLibCheck --types node --outDir scripts && node scripts/generate-houses.js",
    "prebuild": "npm run generate-houses",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "@material/web": "^2.4.1"
  }
}
